import {Appearance} from 'react-native';

const isDark = Appearance.getColorScheme() === 'dark';

export const MyColors = {
  primaryColor: '#d95639',
  secondaryColor: '#fcd36a',
  accentColor: '#f8f8f8',
  lightText: '#8D8989',
};
